# 微信文件群发工具 · 项目总结

> 文档生成时间：2026-08-04
> 项目路径：`C:\Users\10129\WorkBuddy\2026-08-03-10-51-00\`

---

## 1. 项目背景与目标

用户需要在 Windows 上批量地把"某个文件夹里的文件"按规则发给指定的微信联系人：

- 提供一个**映射关系**：文件名中的「关键词」 → 对应的「微信备注名 / 昵称」。
- 程序扫描目标文件夹，把每个文件按关键词匹配到对应联系人。
- 自动打开与该联系人的微信聊天窗口，发送文件（可选附带一条文字消息）。
- 支持一个文件匹配多个关键词 / 一个联系人收多个文件。

核心约束（在多次迭代中明确）：
- 运行在**个人微信**（非企业微信 / 非微信网页版）。
- 映射表**既支持上传 Excel，也支持在界面里直接输入**。
- 文件夹**可点击选择**（系统原生对话框）。
- 最终交付形态：**普通 Windows 原生窗口程序**，无控制台黑框、不自动打开浏览器。

---

## 2. 技术架构

```
┌─────────────────────────────────────────────┐
│            微信文件群发工具.exe               │
│  (PyInstaller --onefile --noconsole 打包)     │
│                                               │
│  ┌────────────────┐      ┌─────────────────┐ │
│  │  pywebview 窗口 │◄────►│  Flask 本地服务  │ │
│  │ (Edge WebView2) │ JS   │  127.0.0.1:5890 │ │
│  │   原生 HTML 界面 │ API  │  (守护线程运行) │ │
│  └────────────────┘      └────────┬────────┘ │
│                                    │ 调用       │
│                          ┌─────────▼─────────┐ │
│                          │  wechat_sender.py  │ │
│                          │  键鼠/剪贴板模拟   │ │
│                          └─────────┬─────────┘ │
│                                    │ 操控       │
│                          ┌─────────▼─────────┐ │
│                          │    Windows 微信客户端 │ │
│                          └───────────────────┘ │
└─────────────────────────────────────────────┘
```

### 关键选型与原因

| 组件 | 作用 | 选型原因 |
|------|------|----------|
| **Flask** | 后端 API + 页面托管 | 轻量，便于把界面和逻辑解耦 |
| **pywebview** | 把 HTML 渲染进原生窗口 | 比 tkinter 好看、比弹浏览器更像"普通程序"；底层用系统 Edge WebView2 |
| **PyInstaller --noconsole** | 打包成单文件 exe | 满足"无控制台、不弹浏览器"的最终要求 |
| **键鼠+剪贴板模拟** | 实际发送动作 | 微信 4.1+ 用 Qt Quick，不暴露 UI Automation 控件，wxauto 等库失效，只能走输入模拟 |
| **win32api / pyautogui** | 窗口查找、置顶、粘贴 | 绕过 Windows 前台锁、处理中文输入 |

> ⚠️ 早期曾尝试 `wxauto4`，因沙箱拦截 + 微信新版不暴露 UIA 控件而彻底放弃，转向输入模拟方案。

---

## 3. 文件结构

```
项目根目录/
├── launch.py                 # pywebview 入口（Flask线程 + 原生窗口）
├── web_app.py                # Flask 后端 + 内嵌 HTML/CSS/JS 前端（主程序）
├── wechat_sender.py          # 独立发送模块（键鼠/剪贴板模拟，核心逻辑）
├── wechat_file_sender.py     # 早期探索版本（已弃用，保留参考）
├── create_template.py        # 生成"发送映射模板.xlsx"
├── 发送映射模板.xlsx          # 关键词 / 微信备注名-昵称 两列表格
├── 启动群发工具.bat           # 旧版带控制台 exe 的启动脚本
├── 微信文件群发工具.spec      # PyInstaller 打包规格
├── build/                    # PyInstaller 中间产物（可删）
├── dist/
│   ├── 微信文件群发工具.exe   # ✅ 最终交付：无控制台原生窗口版
│   └── 发送映射模板.xlsx      # 随附模板
├── test_match_logic.py        # 关键词匹配逻辑单元测试
├── test_web_api.py            # Web API 测试脚本
└── _uploads/                  # 上传的 Excel 临时存放
```

---

## 4. 功能特性

1. **文件夹选择**：界面按钮 → 系统原生文件夹对话框（通过 pywebview JS API）。
2. **两种映射录入方式**：
   - 上传 Excel（关键词列 + 微信备注名/昵称列）。
   - 直接在文本框里按 `关键词,微信备注名/昵称` 逐行输入。
3. **同人合并去重（v1.1 新增）**：多个关键词映射到同一联系人时，自动合并为**一次发送**、文件去重，彻底解决重复发送问题。
4. **双视图实时预览（v1.1 新增）**：
   - 「按联系人」= 实际发送计划（一人一行，已合并去重），**所见即所发**；
   - 「按关键词」= 匹配明细，便于核对每个关键词命中了哪些文件。
5. **可选附言**：发送文件前可填一条文字消息。
6. **发送过程可视化**：前端实时显示进度与每步状态。
7. **停止按钮**：发送中途可中止。
8. **自动窗口管理**：点击「开始发送」后自动切到微信、恢复最小化窗口、置顶，发送结束把焦点拉回本程序。

---

## 5. 工作流程

1. 用户启动 `微信文件群发工具.exe` → 弹出原生窗口（不是浏览器）。
2. 点击「选择文件夹」选定待发送文件所在目录。
3. 录入映射（上传 Excel 或直接输入文本）。
4. 点「预览匹配」→ 确认每个文件对应的收件人。
5. 点「开始发送」→ 程序逐一对每个联系人：
   - 在微信搜索框输入昵称、回车打开聊天；
   - 把该联系人的文件写入剪贴板（CF_HDROP），粘贴发送；
   - 若配置了附言，先粘贴发送文字；
   - 切换下一个联系人，循环直至完成。
6. 发送结束，状态栏汇总成功/失败。

---

## 6. 关键技术方案（难点与实现）

### 6.1 剪贴板传文件（CF_HDROP）
问题：用 `ctypes` + `GlobalAlloc` 传文件列表时，64 位指针被当作 32 位截断，导致 `GlobalLock` 失败。
解决：改用 `win32clipboard.SetClipboardData(win32con.CF_HDROP, data)`，直接传构造好的 bytes，绕开指针长度问题。

### 6.2 中文昵称输入
问题：`pyautogui.write()` 不支持中文（只能输 ASCII）。
解决：微信昵称、附言等所有中文内容一律走「写入剪贴板 → 粘贴（Ctrl+V）」的方式。

### 6.3 切换到微信窗口 / 置顶
问题：在后台线程里 `SetForegroundWindow` 常被 Windows 前台锁拒绝。
解决：
- `AttachThreadInput` 把目标线程与当前线程输入绑定；
- 先按一下 `ALT` 键释放前台锁；
- `ShowWindow(SW_RESTORE)` 恢复最小化窗口；
- 再用 `SetWindowPos(TOPMOST→NOTOPMOST)` + `SetForegroundWindow` 置顶。

### 6.4 无控制台打包
- `PyInstaller --onefile --noconsole` 单文件打包。
- 沙箱会拦截 `build/` 清理（os.remove 走回收站），通过 `CODEBUDDY_SAFE_DELETE_SANDBOX=0` 并将 `--workpath/--distpath` 指向 `%TEMP%` 绕过，构建完成后再把 exe 拷回 `dist/`。
- `hidden-import` 显式声明：wechat_sender、flask、werkzeug、openpyxl、pyautogui、pythoncom、win32*、webview、bottle、proxy_tools、clr_loader、pythonnet。

---

## 7. 问题与修复记录

| 序号 | 现象 | 根因 | 修复 |
|------|------|------|------|
| 1 | wxauto4 初始化失败 | 沙箱拦截 `shutil.rmtree` | monkey-patch（后整体废弃） |
| 2 | wxauto4 连不上微信 | 微信 4.1+ Qt Quick 不暴露 UIA | 彻底弃用，改键鼠模拟 |
| 3 | 剪贴板传文件失败 | 64 位指针被截断 | 改用 `win32clipboard.SetClipboardData` |
| 4 | 发送的人不对 | `pyautogui.write` 不支持中文 | 中文统一走剪贴板粘贴 |
| 5 | 点开始不切窗口 | 后台线程 `SetForegroundWindow` 被拒 | `AttachThreadInput` + ALT 解锁 |
| 6 | 微信最小化后失败 | 恢复/抢前台不稳 | 强制 `SW_RESTORE` + ALT + 置顶 |
| 7 | PyInstaller 清理被拦 | `os.remove` 走回收站 | 设 `SAFE_DELETE_SANDBOX=0` + `%TEMP%` 构建 |
| 8 | GUI"不好用" | 初版 tkinter | 改 Flask Web 界面 |
| 9 | 要求无控制台窗口 | 初版带控制台 exe | pywebview 包装 + `--noconsole` |
| 10 | 模板要额外附带 | exe 与 xlsx 分离 | `--add-data` 编译进资源 + 动态生成兜底 |
| 11 | **同一人重复收到文件**(v1.1) | 发送计划按「映射条目」逐条发，多条映射指向同一人就发多次，且同文件被多关键词命中会重复发 | 改为按联系人聚合去重（`/api/preview` 生成 `grouped`），`send` 只按聚合结果发；映射表重复行一并合并 |
| 12 | 界面文案误导填昵称 | 表头/提示只写「微信昵称」，用户不知可填备注名 | 统一改为「微信备注名/昵称」，补充"优先备注名、重名用微信号"提示 |

---

## 8. 使用说明（面向最终用户）

1. 确认 **微信已登录**，微信窗口在前台或最小化均可（程序会自动处理）。
2. 双击 `dist/微信文件群发工具.exe`，弹出原生窗口（无黑框、不弹浏览器）。
3. 点「选择文件夹」选中待发送文件目录。
4. 在「映射」区上传 Excel 或手动输入 `关键词,微信备注名/昵称`（**优先填备注名**，重名时填微信号最保险）。
5. 点「预览匹配」核对收件人：默认「按联系人」视图就是实际发送计划（同一人已合并为一行、文件已去重），可切到「按关键词」看明细。
6. 点「开始发送」，期间**不要操作鼠标键盘**。
7. 进度完成后查看状态栏汇总。

**环境依赖**
- Windows 10/11（x64）。
- 系统自带或已安装 **Microsoft Edge WebView2 Runtime**（Win10/11 一般自带；若窗口打不开，去微软官网安装 WebView2 Runtime）。
- 个人微信客户端已登录。

---

## 9. 交付物

- ✅ `dist/微信文件群发工具.exe` —— 最终无控制台原生窗口版（约 35MB，单文件，模板已编译进 exe 资源，零附带文件）。
- ✅ 源码：`web_app.py`、`wechat_sender.py`、`launch.py` 等。
- 📄 映射表模板（内嵌于 exe，界面点「下载模板」即可获取）。

---

## 10. 已知限制与后续可优化

1. **依赖输入模拟**：发送时占用键鼠，期间不能手动操作；发送速度受模拟节奏影响（已加适当延时保证稳定）。
2. **联系人靠微信搜索定位**：程序把第二列字符串原样粘进微信搜索框并回车取第一个结果，**不区分备注名/昵称/微信号**——填什么就搜什么。推荐优先级：**微信号（唯一） > 备注名（权重高、可自控） > 原始昵称（易重名、对方改名即失效）**。若重名可能开错会话。可后续加「匹配后人工确认」开关。原界面/模板仅写「微信昵称」，v1.1 已改为「微信备注名/昵称」并补充填写建议。
3. ~~模板未内置~~ ✅ **已解决**：模板已通过 PyInstaller `--add-data` 编译进 exe 资源，`dist/` 仅一个 exe 即可运行；界面新增「下载模板」按钮，运行时优先读取内嵌资源，读取失败则用 openpyxl 动态生成兜底。
4. ~~多关键词映射同一人导致重复发送~~ ✅ **已解决（v1.1）**：发送计划改为按联系人聚合去重，同一人只开一次会话、只发一次；预览新增「按联系人」视图，所见即所发。
5. **无自动重试**：单个文件发送失败会记录但跳过，可加失败重试队列。
6. **仅 Windows**：方案深度绑定 Win32 API，无法跨平台。
